import express from "express";
const router = express.Router();

router.get("/", (async) => ({}));
router.get("/:id", (async) => ({}));
router.post("/", (async) => ({}));
router.put("/:id", (async) => ({}));
router.delete("/:id", (async) => ({}));

export default router;
